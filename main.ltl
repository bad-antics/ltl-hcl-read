// ltl-hcl-read/main.ltl
//
// HashiCorp HCL2 reader for Terraform-style configs.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-hcl-read v0.1.0")
    io::println("HashiCorp HCL2 reader for Terraform-style configs.")
    0
}
