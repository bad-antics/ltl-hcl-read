// examples/demo.ltl — runnable demo for ltl_hcl_read

import std::io
use ltl_hcl_read as lib

fn main() {
    io::println("ltl_hcl_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}